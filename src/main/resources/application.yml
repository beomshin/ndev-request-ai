# 서버 설정
server:
  port: 8080

spring:
  application:
    name: nice-qa

# LLM 설정 (환경변수 LLM_BASE_URL, LLM_MODEL, LLM_API_KEY 로 덮어쓰기 가능)
llm:
  base-url: ${LLM_BASE_URL:https://placeholder.example.com/v1}
  model: ${LLM_MODEL:placeholder-model}
  api-key: ${LLM_API_KEY:placeholder-api-key}
